PWAアプリ起動時の認証問題を修正

- エンドポイントの不一致を修正（/api/users/${id}/profile → /api/profile?user_id=${id}）
- レスポンス構造の不一致を修正（profileResponse.name → profileResponse.profile.name）
- トークン検証失敗時のエラーハンドリングを強化（必ずlocalStorageをクリアして認証状態をリセット）
- ログイン前に既存のlocalStorageをクリアするように改善
- デバッグログを追加して問題の原因を特定しやすく改善
